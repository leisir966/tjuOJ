#include <vector>
#include <iostream>
using namespace std;

int main(){
	int i;
	cin>>i;
	struct node 
	{    int agus,agiv,bgus,bgiv }; 
	vector node[i];
	int fa=0,fb=0;
	while(i--){
		scanf("%d%d%d%d",&agus,&agiv,&bgus,&bgiv)
		for(int j=0;j<=3;j++)
			{
			v[0].push_back()
			}
		if (vector[a[0]]+vector[a[2]]==vector[a[3]])
		{
			fa++;
		}else if (vector[a[0]]+vector[a[2]]==vector[a[1]])
		{
			fb++;
		}
}
printf("%d %d",fa,fb);
	return 0;
	
}
